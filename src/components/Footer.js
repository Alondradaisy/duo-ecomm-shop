import React from "react";
import Box from "@mui/material/Box";

const Footer = (props) => {
  return (
    <Box sx={{ p: 5, background: "grey", textAlign: "center" }}>Footer</Box>
  );
};

export default Footer;
